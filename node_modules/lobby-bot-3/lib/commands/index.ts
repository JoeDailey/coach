import { Slash, SlashCommand } from 'discord-slash';
import LobbySlashCommand from './lobby';

export function commands(): Slash {
  return new Slash(
    [LobbySlashCommand.name, LobbySlashCommand],
    SlashCommand.alias('lb', LobbySlashCommand),
  );
}

