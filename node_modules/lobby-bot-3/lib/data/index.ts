import Redis from 'ioredis';

const port = parseInt(process.env.REDIS_PORT ?? '6379');
const host = process.env.REDIS_HOST ?? "127.0.0.1";
const prefix = process.env.REDIS_PREFIX ?? "lb3:"
export const redis = new Redis({port, host});