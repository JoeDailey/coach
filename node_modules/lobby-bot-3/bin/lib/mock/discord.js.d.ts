import { Client, ClientOptions, Guild, TextChannel, User } from "discord.js";
import { RawGuildChannelData, RawGuildData, RawUserData } from "discord.js/typings/rawDataTypes";
export declare const Mock: {
    Client: (options: ClientOptions) => Client;
    Guild: (client: Client, data: RawGuildData) => Guild;
    TextChannel: (guild: Guild, data?: RawGuildChannelData, client?: Client, immediatePatch?: boolean) => TextChannel;
    User: (client: Client, data?: RawUserData) => User;
};
