"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const discord_slash_1 = require("discord-slash");
const lobbies_1 = require("../../lobbies");
exports.default = new discord_slash_1.SlashSubcommand("add", "Add a player to the lobby", handle, cmd => cmd
    .addUserOption(member => member
    .setName('member')
    .setDescription('Player to add')
    .setRequired(true)));
async function handle(interaction) {
    const user = interaction.options.getMember('member');
    const channel = interaction.channel;
    const lobby = await lobbies_1.Lobby.get(channel);
    lobby.addPlayers(user);
    await interaction.reply(await lobby.print(channel));
}
//# sourceMappingURL=add.js.map